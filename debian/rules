#!/usr/bin/make -f
include /usr/share/cdbs/1/rules/debhelper.mk

build/blankon-sounds::
	mkdir -p build/etc/skel/.gnome/sound/events
	cp -p *.soundlist build/etc/skel/.gnome/sound/events
	mkdir -p build/usr/share/sounds/blankon
	cp -p *.wav build/usr/share/sounds/blankon

clean::
	rm -rf build
